---
phase: 01-coderabbit-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [".coderabbit.yml"]
autonomous: true
requirements: ["CR-01", "CR-02", "CR-03", "CR-04"]
must_haves:
  truths:
    - "A valid .coderabbit.yml configuration file exists in the root of the repository"
    - "CodeRabbit is configured to ignore draft PRs and disable creative summaries/poems"
    - "TypeScript files have custom rules checking for WebGL context sharing and matrix alignments"
    - "Styling and HTML files have custom rules checking glassmorphism and viewport layout settings"
  artifacts:
    - path: ".coderabbit.yml"
      provides: "CodeRabbit review rules and preferences"
      contains: "file_path_instructions"
---

<objective>
Configure and enable automated code review rules using CodeRabbit on pull requests.

Purpose: Customize CodeRabbit's automated PR review behavior specifically for the Mirror World spatial renderer.
Output: A `.coderabbit.yml` file with tailored TypeScript and Styling review instructions.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-coderabbit-integration/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Create .coderabbit.yml configuration</name>
  <files>.coderabbit.yml</files>
  <read_first>DESIGN.md, src/main.ts</read_first>
  <action>
    Create a new `.coderabbit.yml` in the root of the repository.
    Define the following configuration properties:
    - `language: "en-US"`
    - `ignore_draft_pr: true`
    - `disable_poem: true` (or `poem: false` as per CodeRabbit syntax)
    
    Add `file_path_instructions`:
    1. For `src/**/*.ts`:
       - Instruct CodeRabbit to ensure Three.js WebGLRenderer shares the MapLibre GL context: `context: gl` on initialization.
       - Instruct CodeRabbit to check that `renderer.resetState()` is called on every frame rendering step to prevent canvas context corruption.
       - Instruct CodeRabbit to ensure coordinate translations are handled via `MercatorCoordinate` and local scale meters to avoid float precision limits.
       - Instruct CodeRabbit to check that visual parameter updates call `map.triggerRepaint()` to force map refreshes.
       
    2. For `src/**/*.css, index.html`:
       - Instruct CodeRabbit to verify absolute panel layout position constraints, HSL colors, and glassmorphic aesthetics (backdrop blurs, semi-transparent borders).
       - Instruct CodeRabbit to verify unique DOM element IDs for telemetry panels.
       - Instruct CodeRabbit to verify mobile navigation tab responsiveness.
  </action>
  <verify>test -f .coderabbit.yml</verify>
  <acceptance_criteria>
    - `.coderabbit.yml` exists in the repository root.
    - `.coderabbit.yml` contains `ignore_draft_pr: true`.
    - `.coderabbit.yml` contains instructions mapping `src/**/*.ts`.
    - `.coderabbit.yml` contains instructions mapping `src/**/*.css`.
  </acceptance_criteria>
  <done>CodeRabbit configuration file is written and fully configured</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify `.coderabbit.yml` file is syntactically valid YAML.
- [ ] Confirm all customized guidelines are properly spelled and scoped to the correct file matches.
</verification>

<success_criteria>
- All tasks completed.
- The `.coderabbit.yml` configuration is verified and committed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-coderabbit-integration/01-01-SUMMARY.md`
</output>
